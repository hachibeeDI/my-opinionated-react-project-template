{
  "extends": "./.babelrc.base",
  plugins: [
    ["i18next-extract", {
      outputPath: 'src/locales/{{locale}}/{{ns}}.json',
      locales: ['en', 'ja'],
      defaultNS: 'commons',
      discardOldKeys: true,
    }],
  ]
}
